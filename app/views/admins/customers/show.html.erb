<h2><%= "#{@customer.first_name}#{@customer.last_name}" %>さんの会員詳細</h2>
<table>
  <tbody>
    <tr>
      <td>会員ID　　　　　<%= @customer.id %></td>
      <td>氏名　　　　　　<%= "#{@customer.first_name}#{@customer.last_name}" %></td>
      <td>フリガナ　　　　<%= "#{@customer.first_name_kana}#{@customer.last_name_kana}" %></td>
      <td>郵便番号　　　　<%= @customer.postal_code %></td>
      <td>住所　　　　　　<%= @customer.address %></td>
      <td>電話番号　　　　<%= @customer.telephone_number %></td>
      <td>メールアドレス　<%= @customer.email %></td>
      <td>会員ステータス　<%= @customer.is_deleted ? '有効' : '退会' %></td>
    </tr>
  </tbody>
</table>
<%= link_to "編集する", edit_admins_customer_path %>　<%= link_to "注文履歴一覧を見る", admins_root_path %>