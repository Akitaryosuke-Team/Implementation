<h2>ジャンル編集</h2>
  <%= form_with model: @genre, url: admins_genre_path, method: :patch do |f| %>
    <div class="field">
      <%= f.label :"ジャンル名" %>
      <%= f.text_field :name %>
    </div>

    <div class="actions">
      <%= f.submit '変更を保存' %>
    </div>
  <% end %>